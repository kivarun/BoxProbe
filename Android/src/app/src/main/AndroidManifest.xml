<?xml version="1.0" encoding="utf-8"?>
<!--
 Copyright 2025 Google LLC

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

     http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk
        android:minSdkVersion="35"
        android:compileSdkVersion ="36"
        android:targetSdkVersion="36" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.apps.aicore.service.BIND_SERVICE" />

    <application
        android:name="com.box.gallery.GalleryApplication"
        android:allowBackup="false"
        android:hasFragileUserData="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="BoxProbe"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gallery"
        android:memtagMode="async"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="36">
        <!--
          android:configChanges="uiMode" tells the system don't destroy and
          recreate the activity when UI mode changes (e.g. setting dark mode).
          Instead, just recompose the view.
         -->
        <activity
            android:name="com.box.gallery.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Gallery"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="uiMode"
            tools:ignore="DiscouragedApi">
            <!-- Security: FLAG_SECURE prevents screenshots/screen recording -->
            <meta-data android:name="android.activity.windowSecure" android:value="true" />
            <!-- This is for putting the app into launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- This is for deep linking (exact authority keeps AppAuth redirects for
                 scheme com.kivarun.boxprobe exclusive to RedirectUriReceiverActivity) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="com.kivarun.boxprobe"
                    android:host="global_model_manager" />
            </intent-filter>
        </activity>

        <!-- Set themes for activities that are used for viewing open source licenses -->
        <activity
            android:name="com.google.android.gms.oss.licenses.OssLicensesMenuActivity"
            android:exported="true"
            android:theme="@style/Theme.Gallery.OssLicenses" />
        <activity
            android:name="com.google.android.gms.oss.licenses.OssLicensesActivity"
            android:exported="true"
            android:theme="@style/Theme.Gallery.OssLicenses" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <uses-native-library android:name="libvndksupport.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL.so" android:required="false"/>
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />

        <!-- MediaTek Neuron runtime: the self-contained LiteRT MediaTek dispatch
             dlopens these preinstalled libraries from /system_ext. Without these
             entries the app namespace cannot resolve them and NPU init fails
             with "Failed to load NeuronAdapter shared library". -->
        <uses-native-library android:name="libneuronusdk_adapter.mtk.so" android:required="false"/>
        <uses-native-library android:name="libneuronusdk_adapter.9.mtk.so" android:required="false"/>
        <uses-native-library android:name="libneuron_sys_util.mtk.so" android:required="false"/>
        <uses-native-library android:name="libneuron_adapter_mgvi.so" android:required="false"/>
    </application>

</manifest>
